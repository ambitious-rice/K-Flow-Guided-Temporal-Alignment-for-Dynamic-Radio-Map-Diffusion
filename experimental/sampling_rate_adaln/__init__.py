"""W1 sampling-rate conditioning experiment.

The experiment code lives here because the historical W1 implementation is a
read-only runtime dependency on the remote machine.  Nothing in this package
edits or replaces those legacy modules.
"""
