# AGENTS.md

## 1. 工作模式

本项目采用“本地开发 + 远程执行”的工作方式。

本地节点负责：

* 源代码开发与修改
* Git 管理
* 本地静态检查、单元测试和轻量 smoke test
* 实验配置维护
* 运行记录与状态持久化
* 结果同步后的分析、统计和可视化
* 所有需要修改源码的工作

远程节点负责：

* Python / Conda / CUDA 等运行环境配置
* 安装和更新依赖
* 下载模型、数据集及其他运行资源
* GPU/CPU 计算任务
* 长时间任务运行
* 日志、checkpoint 和原始结果保存
* 结果打包与同步
* GPU、磁盘、进程等运行状态检查

Codex 可以通过 SSH 自由操作远程节点，但必须遵守本文中的源码和状态管理规则。

---

## 2. 基本原则

### 2.1 本地源码是唯一真源

项目源码以本地 Git 仓库为准。

远程节点上的项目源码只用于运行。

禁止直接在远程修改受 Git 管理的项目源码，包括但不限于：

* 使用 vim / nano 修改 Python 文件
* 使用 sed / perl 等命令直接修改源码
* 临时复制一个修改后的源码版本进行运行
* 在远程创建只存在于该服务器上的研究代码分支

如果远程运行中发现代码问题：

1. 在远程读取日志和代码定位问题
2. 回到本地修改源码
3. 在本地完成必要检查
4. 提交或记录修改
5. 将新版本同步到远程
6. 使用新版本重新运行

远程环境配置文件、shell 配置、缓存、下载脚本以及不属于项目源码的机器级配置可以直接修改。

---

### 2.2 Codex 可以自主使用 SSH

Codex 可以根据任务需要直接调用 SSH，不需要等待用户手工登录远程服务器。

允许执行的远程操作包括但不限于：

* 查看目录和文件
* 检查 Git 状态
* git fetch / pull / checkout
* 创建和维护 Conda 环境
* pip / conda 安装依赖
* 检查 CUDA、PyTorch、编译器等环境
* 下载模型权重和数据
* 创建运行目录
* 启动、停止和恢复任务
* 使用 tmux、screen、nohup、torchrun 等工具
* 查看日志
* 检查进程
* 检查 GPU 使用情况
* 检查磁盘空间
* 打包结果
* 上传或下载结果
* 清理缓存和无用临时文件

操作远程服务器时，应优先直接检查真实状态，不要根据之前的聊天记录推测服务器当前状态。

---

## 3. 项目配置

项目相关的固定资源信息应保存在：

`.agents/config.yaml`

其中可以包括：

* SSH host
* 远程 workspace
* Conda 环境路径或名称
* 数据集路径
* 模型权重路径
* 远程结果目录
* 本地结果目录
* Git 仓库信息
* 网盘或其他同步位置

开始涉及远程操作的任务前，应优先读取该配置。

不要把这些信息仅保存在聊天上下文中。

如果发现配置发生长期变化，应更新配置文件。

---

## 4. 状态持久化

重要实验和长时间任务的状态必须写入项目文件。

不要依赖 Codex 聊天记录保存以下信息：

* 当前正在运行什么
* 为什么运行
* 使用了哪个代码版本
* 使用了什么命令
* PID
* tmux/session 名称
* 日志在哪里
* 结果在哪里
* 下一步准备做什么

运行记录统一保存在：

`.agents/runs/`

推荐每个重要任务建立一个独立 YAML 文件，例如：

`.agents/runs/20260831_w16_da_sigma10.yaml`

至少记录：

```yaml
id:
goal:

source:
  commit:
  branch:
  dirty:

remote:
  host:
  workdir:

execution:
  command:
  environment:
  pid:
  session:
  log:
  started_at:

status:

artifacts:
  remote:
  local:

notes:
next_action:
```

字段可以根据实际任务增加或删除，不要求机械保持完全一致。

---

## 5. 哪些任务需要记录

以下情况通常应建立 run 记录：

* GPU 训练
* 大规模 evaluation
* 长时间推理
* 数据生成
* 数据预处理
* 大规模 benchmark
* 重要模型下载或转换
* 预计离开当前聊天后仍会继续运行的任务
* 之后可能需要复现实验结果的任务

几秒钟完成的简单检查、临时 shell 查询、环境探测等不需要创建 run。

避免为了形式化而记录大量没有长期价值的小操作。

---

## 6. 启动远程任务前

对于重要计算任务，启动前应根据实际情况检查：

### 本地

* 当前代码修改是否符合预期
* Git 状态
* 必要的静态检查
* 必要的单元测试
* 可行时执行轻量 smoke test
* 实验配置是否正确

不要求本地完整复现远程 GPU 环境。

如果任务规模过大，本地只需完成足以发现明显代码问题的检查。

### 远程

根据任务需要检查：

* SSH 是否正常
* 正确代码版本是否已经部署
* Git working tree 是否符合预期
* Conda/Python 环境
* CUDA / GPU
* 数据集
* 模型权重
* 磁盘空间
* 输出目录
* 是否存在冲突任务

检查内容应服务于当前任务，不需要每次机械执行完整检查列表。

---

## 7. Git 与远程代码

优先通过 Git 同步代码。

由于远程服务器可以访问互联网，应优先使用 HTTPS Git 访问方式。

运行重要实验时，应尽可能记录具体 commit。

如果远程仓库存在未提交的源码修改，应先确认来源。

不要在不清楚修改来源的情况下：

* 覆盖这些修改
* 将这些修改混入新的实验
* 假设这些修改可以安全删除

如果这些修改属于之前遗留的远程临时代码，应优先恢复到可追踪的 Git 状态。

---

## 8. 未提交的本地代码

允许在必要情况下运行未提交代码，但必须能够恢复运行时的真实代码状态。

优先选择提交 Git commit。

如果由于实验性质暂时不适合 commit，应至少记录：

* 当前 commit
* dirty 状态
* 必要时保存 diff / patch

不要让某个重要实验只依赖后来已经消失的本地未提交修改。

---

## 9. 环境管理

远程运行环境允许 Codex 自主维护。

可以：

* 创建 Conda 环境
* 安装缺失依赖
* 解决 Python package 冲突
* 调整 CUDA 相关依赖
* 编译必要 extension
* 配置 cache
* 配置环境变量

环境问题应尽量在环境层解决。

不要为了绕过依赖或环境问题直接修改研究源码，除非源码本身确实存在问题。

如果环境发生可能影响实验复现的重要变化，应在运行记录或项目配置中留下说明。

---

## 10. 任务运行

长时间任务应使用适合脱离 SSH session 继续运行的方式，例如：

* tmux
* screen
* nohup
* 调度系统

选择方式根据远程机器实际情况决定。

启动后应确认任务确实开始运行，而不是仅确认命令成功提交。

必要时检查：

* PID
* session
* 日志是否产生
* GPU 是否开始使用
* 是否立即发生异常

随后将真实运行信息写入对应 run 文件。

---

## 11. 任务状态检查

用户询问某个任务是否完成、运行到哪里或是否正常时：

优先通过 SSH 查询真实远程状态。

可以根据情况检查：

* ps
* tmux / screen
* nvidia-smi
* 日志尾部
* checkpoint
* 输出文件数量
* 程序自身 progress
* exit code

不要仅根据 `.agents/runs/` 中之前记录的 `running` 或聊天中的历史信息判断。

run 文件表示持久记录，远程机器表示当前事实。

查询完成后，如果状态发生重要变化，应更新对应 run 文件。

---

## 12. 结果同步

远程负责产生原始结果，本地负责主要分析。

优先同步：

* metrics
* csv/json
* 日志
* 图像或视频预览
* 必要 checkpoint
* 实验配置
* 运行 metadata

不应默认把所有缓存、中间 tensor、临时文件全部同步回本地。

对于大型结果，应根据当前研究目的判断需要同步哪些内容。

代码优先使用 Git，不使用网盘作为主要代码同步机制。

大型数据、模型、checkpoint 和生成结果可以通过网盘或其他外部存储同步。

---

## 13. 本地分析

结果同步完成后，以下工作原则上在本地完成：

* 指标整理
* CSV 处理
* 结果比较
* 绘图
* 表格生成
* 可视化
* 实验总结
* 错误案例分析
* 论文数据整理

如果某项分析本身需要大量 GPU 计算，可以作为新的远程计算任务处理。

---

## 14. 新会话恢复上下文

当 Codex 进入一个新的聊天或缺少之前上下文时，不应要求用户重新描述整个项目状态。

优先自行读取：

1. `AGENTS.md`
2. `.agents/config.yaml`
3. `.agents/runs/`
4. Git status / log
5. 与当前任务相关的项目文档

如果涉及正在运行的任务，再通过 SSH 查询真实状态。

项目文件中的持久信息优先于聊天历史。

---

## 15. 工作记录的粒度

目标是保留未来真正需要的信息，而不是记录所有操作过程。

应该记录：

* 为什么运行
* 跑了什么
* 用的什么版本
* 在哪里运行
* 结果在哪里
* 当前是什么状态
* 接下来准备做什么

通常不需要记录：

* 每一条 ls
* 每一次 cd
* 临时诊断命令
* 没有产生长期影响的排错过程

除非这些信息对复现某个问题很重要。

---

## 16. 自主性

在本文规则范围内，Codex可以自主完成：

* 环境检查
* 环境配置
* SSH 操作
* Git 同步
* 任务启动
* 状态检查
* 日志诊断
* 结果整理和同步

不需要为每一个常规 shell 操作向用户请求确认。

涉及以下情况时应谨慎：

* 删除大量数据
* 删除 checkpoint
* 覆盖已有结果
* 修改远程其他用户的数据
* 修改系统级配置
* 执行不可逆操作
* 无法判断某个已有文件或进程是否仍有用途

---

## 17. 总体约束

始终保持以下边界：

**本地负责开发。**

**Git 负责代码版本。**

**SSH 负责远程控制。**

**远程负责计算。**

**外部存储负责大型文件传输。**

**项目文件负责状态持久化。**

**聊天记录不作为项目状态的唯一来源。**

在不违反这些边界的前提下，根据实际任务自由选择最合适的 shell 命令和工作方式。
